language: android
android:
  components:
    - build-tools-21.1.1
before_install:
    - chmod +x gradlew
    # for gradle output style
    - export TERM=dumb

    # environment info
    - ./gradlew -v
    - uname -a
# Let's try to build...
script: ./gradlew clean build